# RLC website

## Local preview

Requires Python 3. Install dependencies and start the preview:

```sh
python3 -m pip install -r requirements.txt
python3 preview.py
```

The site opens at <http://localhost:8000>. Use `python3 preview.py --no-open` to prevent opening a browser, or pass a port such as `python3 preview.py 5000`.
